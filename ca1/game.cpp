#include <iostream>
#include <string>
#include <vector>
using namespace std;

class GameState {
  vector<vector<char>> squares;
  
  GameState(int size) {
    vector<vector<char>> squares;
    for (int x = 0; x < size*2; x += 1) {
      squares.push_back(vector<char>());
      for (int y = 0; y < size*2; y += 1) {
        squares.at
      }
    }
  }

};
